---
# Example: Tailscale Action with OIDC Authentication
#
# This workflow demonstrates how to use the Tailscale GitHub Action with
# OIDC authentication, including the necessary token exchange steps.
#
# Prerequisites:
# 1. Configure Tailscale Trust Credential (see .github/docs/OIDC_SETUP.md)
# 2. Set repository secrets:
#    - TS_V2_OIDC_CLIENT_ID: Your Tailscale OIDC client ID
#    - TS_V2_OIDC_AUDIENCE: Your custom audience (e.g., "tailscale-ci")
#
# For complete setup instructions, see: .github/docs/OIDC_SETUP.md

name: Example Simple Tailscale Action

on:
  workflow_dispatch:  # Manual trigger for testing

permissions:
  contents: read      # Required for actions/checkout
  id-token: write     # Required for OIDC token generation

jobs:
  simple-tailscale-example:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Get GitHub OIDC token
        id: get_oidc_token
        run: |
          OIDC_TOKEN=$(curl -sS -H "Authorization: Bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "${ACTIONS_ID_TOKEN_REQUEST_URL}&audience=${{ secrets.TS_V2_OIDC_AUDIENCE }}" | jq -r '.value')
          echo "token=$OIDC_TOKEN" >> $GITHUB_OUTPUT
          echo "::add-mask::$OIDC_TOKEN"

      - name: Exchange OIDC token for Tailscale API token
        id: get_ts_api_token
        run: |
          RESPONSE=$(curl -sS -X POST https://api.tailscale.com/api/v2/oauth/token-exchange \
            -H "Content-Type: application/x-www-form-urlencoded" \
            -d "client_id=${{ secrets.TS_V2_OIDC_CLIENT_ID }}" \
            -d "jwt=${{ steps.get_oidc_token.outputs.token }}")
          ACCESS_TOKEN=$(echo "$RESPONSE" | jq -r '.access_token')
          if [ "$ACCESS_TOKEN" = "null" ] || [ -z "$ACCESS_TOKEN" ]; then
            echo "Failed to get Tailscale API token. Check OIDC configuration."
            exit 1
          fi
          echo "access_token=$ACCESS_TOKEN" >> $GITHUB_OUTPUT
          echo "::add-mask::$ACCESS_TOKEN"

      - name: Connect to Tailscale Network
        uses: tailscale/github-action@v2
        with:
          oauth-client-id: ${{ secrets.TS_V2_OIDC_CLIENT_ID }}
          oauth-secret: ${{ steps.get_ts_api_token.outputs.access_token }}
          tags: tag:github-actions

      - name: Verify Tailscale connection
        run: |
          echo "✅ Successfully connected to Tailscale!"
          tailscale status --peers=false
          echo "IP: $(tailscale ip -4)"

      - name: Test Tailscale Connection
        run: |
          echo "🔗 Connected to Tailscale network!"
          tailscale status --json | jq '.User, .Tailnet, .Self.IPs'

      - name: Your CI/CD Steps Here
        run: |
          # Add your application deployment, testing, or other CI/CD steps
          echo "🚀 Ready for your CI/CD pipeline!"
          echo "This machine is now part of your Tailscale network."
